import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://example.com',
  // Reconstruido como framework propio a partir de la ESTRUCTURA de
  // encounterministries.us (WordPress + Divi). Código limpio, no clonado.
});
